# COOLors

Project for colors generation. Generates 5 random colors, that can be fixed. The resulting palette can be transfered by sending URL.

To change colors press Space. To lock color click on a lock.
